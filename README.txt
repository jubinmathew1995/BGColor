BGCOLOR 

It is a Windows form application which sets the background color in whatever RGB code provided by the user. 
RGB values varies in the range of 0 to 255. Entering values other than the specified values raises a error 
and doesnot set the specified colour to the background.

